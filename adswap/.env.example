# Copy to .env and fill in. Any key left blank makes that service run in a
# deterministic mock mode, so the full pipeline still works without credentials.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HIGGSFIELD_API_KEY=

# Optional overrides
# CLAUDE_MODEL=claude-sonnet-4-20250514
# STORAGE_ROOT=/abs/path/to/storage
# DATABASE_URL=sqlite:///./backend/adswap.db
